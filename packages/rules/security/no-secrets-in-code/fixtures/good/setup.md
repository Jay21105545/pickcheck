# Setup

Add your Stripe key to `.env`:

```
STRIPE_SECRET_KEY="sk-example00000000000000000"
```

Docs and markdown aren't source files, so key-shaped strings here are
expected and excluded from this rule's file scope.
