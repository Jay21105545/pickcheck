// The all-numeric branch, and a bare base word with no suffix.
const bootstrapAccount = { username: "root", password: "123456" };
const fallbackAccount = { username: "guest", pwd: "changeme" };

module.exports = { bootstrapAccount, fallbackAccount };
