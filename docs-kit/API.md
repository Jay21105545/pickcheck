# API — {{PROJECT_NAME}}

No endpoints are documented yet.

Run `npx pickcheck gen api` from the repo root — it embeds your real route
code into a prompt (`pickcheck-prompt.md`) that your AI assistant can turn
into this document. pickcheck itself never calls an LLM or the network.
